# smalltv-mod ESP32-C2 (ESP8684) 4 MB flash layout.
# Two OTA app slots (manual web-UI OTA needs room for a second copy) + ~0.9 MB
# LittleFS for /config.json. FS partition is subtype 'spiffs' so the Arduino
# LittleFS.begin() default label lookup finds it with no code change.
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xE000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x180000,
app1,     app,  ota_1,   0x190000, 0x180000,
spiffs,   data, spiffs,  0x310000, 0xF0000,
