# smalltv-mod classic ESP32 8 MB flash layout.
# Matches the stock SmallTV Pro table (verified from a live device via
# /space.json, see GeekTV-Pro/partitions.csv): two OTA app slots of 0x220000
# (2.125 MB) each + 0x3B0000 (~3.7 MB) LittleFS. FS partition is subtype
# 'spiffs' so the Arduino LittleFS.begin() default label lookup finds it with
# no code change.
# Name,   Type, SubType, Offset,   Size,     Flags
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xE000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x220000,
app1,     app,  ota_1,   0x230000, 0x220000,
spiffs,   data, spiffs,  0x450000, 0x3B0000,
