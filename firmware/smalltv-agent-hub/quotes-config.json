{
  "_comment": "Example symbol list for .github/scripts/fetch-quotes.mjs, the optional fetcher behind the firmware's 'GitHub' ticker source. It writes one static JSON file per cash.ch listing key for publication from a data branch. This repository does not run a publishing workflow. Copy and schedule the script in your own fork, then set a ticker's source to GitHub and its symbol to the listing key. The example below is a placeholder.",
  "range": "6mo",
  "points": 48,
  "symbols": [
    { "key": "123456789-246-333", "name": "Example instrument" }
  ]
}
